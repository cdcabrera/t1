import { One } from './one';
import { Two } from './two';
import { sampleFunction } from './sampleFunction';

const components = {
  One,
  Two
};

export { components as default, One, Two, sampleFunction };
